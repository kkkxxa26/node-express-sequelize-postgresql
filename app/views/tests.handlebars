<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Тест: Математика</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f7fa;
      padding: 24px;
    }

    .form-container {
      max-width: 700px;
      margin: auto;
      background: white;
      border-radius: 8px;
      padding: 32px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    h2 {
      margin-bottom: 24px;
      color: #333;
    }

    .question {
      margin-bottom: 32px;
    }

    .question label {
      display: block;
      margin-bottom: 8px;
      font-weight: bold;
      color: #3c4043;
    }

    .options input[type="radio"] {
      margin-right: 10px;
    }

    .options label {
      margin-bottom: 8px;
      cursor: pointer;
    }

    .submit-button {
       
      padding: 8px 16px;
      width: 240px;
      height: 32px;
      background-color: #4caf50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      text-align: center;
      justify-content: center;
      font-size: 14px;
      text-decoration: none !important; /* Убираем подчеркивание */
      display: inline-block; /* Чтобы width: 100% работал корректно */
      transition: background-color 0.3s ease;
    }
     .submit-button a {
    text-decoration: none;
    color: inherit;
    display: block;
    }

    .submit-button:hover {
      background-color: #155ea9;
    }
  </style>
</head>
<body>

  <div class="form-container">
    <h2 id="test-title">Тест: Математика</h2>

    <div id="q-list">

    
    <!-- Вопрос 1 -->
    <div class="question">
      <label for="q1">1. Чему равно 2 + 2?</label>
      <div class="options">
        <label><input type="radio" name="q1" value="a"> a) 3</label>
        <label><input type="radio" name="q1" value="b"> b) 4</label>
        <label><input type="radio" name="q1" value="c"> c) 5</label>
      </div>
    </div>

    <!-- Вопрос 2 -->
    <div class="question">
      <label for="q2">2. Как называется результат умножения?</label>
      <div class="options">
        <label><input type="radio" name="q2" value="a"> a) Произведение</label>
        <label><input type="radio" name="q2" value="b"> b) Сумма</label>
        <label><input type="radio" name="q2" value="c"> c) Разность</label>
      </div>
    </div>

    <!-- Вопрос 3 -->
    <div class="question">
      <label for="q3">3. Какое число делится на все числа без остатка?</label>
      <div class="options">
        <label><input type="radio" name="q3" value="a"> a) 0</label>
        <label><input type="radio" name="q3" value="b"> b) 1</label>
        <label><input type="radio" name="q3" value="c"> c) 10</label>
      </div>
    </div>

    </div>

    <!-- Кнопка отправки -->

     <div class="card-actions">
          <button  type="button" id="butten-end"  class="submit-button" >
            {{!-- <a href="./mainpage">Завершить тест</a> --}}
            Завершить тест
            </button>
          </div>


<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
  $(document).ready(function () {

    const queryParams = new URLSearchParams(window.location.search);

    // Получаем конкретный параметр, например id
    const id = queryParams.get('id');
    $.ajax({
      url: '/api/lab/oneHead/'+id,
      method: 'GET',
      dataType: 'json',
      success: function (data) {
        $('#test-title').html(data.title);
        const container = $('#q-list');
        container.empty();

        data.lab_bodies.forEach(function (question) {
          var card = `
            <div class="question">
              <label for="q1">${question.title}</label>
              <div class="options">
          `;

          question.lab_answers.forEach((ans)=>{
            card+=`<label><input type="radio" name="${question.id}" value="${ans.id}">${ans.title}</label>`;
          })


          card+=` </div>
            </div>`;

          container.append(card);
        });
        
      },
      error: function () {
        //$('#lecture-list').html('<p>Ошибка загрузки лекций.</p>');
      }
    });
  });


  $('#butten-end').on('click',(e)=>{
    $('input[type="radio"]:checked').each(function() {
      var  lab_body_Id = $(this).attr('name');   
      var lab_answers_Id = $(this).val();        
      // console.log(`id-q: ${name}, id-a: ${value}`); 

      var answers = {
        lab_body_Id:  lab_body_Id,
        lab_answers_Id: lab_answers_Id
      }

       $.ajax({
      url: '/api/userAnswer',
      method: 'POST',
      dataType: 'json',
      data: JSON.stringify(answers),
      contentType: 'application/json',
      success: function (data) {
        console.log(`nice`); 

      },
   error: function () {
        //$('#lecture-list').html('<p>Ошибка загрузки лекций.</p>');
        console.log('error')
      }
    });

    });
  });
</script>

</body>
</html>